# --- Core API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1

# --- LLM access (provider-agnostic: OpenAI / Anthropic / Ollama / vLLM) ---
litellm==1.57.4

# --- Vector store + local embeddings ---
chromadb==0.6.3
sentence-transformers==3.3.1

# --- Data loading ---
openpyxl==3.1.5

# --- Tests ---
pytest==8.3.4
# httpx is used by FastAPI's TestClient. Pinned <0.28 because litellm requires it.
httpx==0.27.2
